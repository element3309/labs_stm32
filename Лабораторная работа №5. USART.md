# Лабораторная работа №5.
## UART + DMA + sprintf

## Файл lab5_usart.h

```c
#ifndef SRC_PLATFORM_LAB5_USART_LAB5_USART_H_
#define SRC_PLATFORM_LAB5_USART_LAB5_USART_H_

#include <stdint.h>
#include "platform.h"

void lab5_usart_init(void);
void lab5_usart_process(void);

#endif
```
## Файл lab5_usart.c 
```c
#include "lab5_usart.h"
#include "usart.h"
#include <string.h>
#include <stdio.h>

static uint32_t press_counter = 0;
static char tx_buffer[100];

static void uart_send_dma(const char* str)
{
    HAL_UART_Transmit_DMA(&huart1, (uint8_t*)str, strlen(str));
}

void lab5_usart_init(void)
{
    uart_send_dma("Lab5: DMA + sprintf ready\r\n");
}

void lab5_usart_process(void)
{
    static uint32_t last_btn_state = 1;
    uint32_t btn_state = HAL_GPIO_ReadPin(GPIOC, GPIO_PIN_13);
    
    if(btn_state == 0 && last_btn_state == 1)
    {
        press_counter++;
        
        sprintf(tx_buffer, "Press: %lu | HEX: 0x%lX | Time: %lu ms\r\n",
                press_counter, press_counter, HAL_GetTick());
        
        uart_send_dma(tx_buffer);
    }
    
    last_btn_state = btn_state;
}
```
## Файл platform.c 
```c
#include "platform.h"
#include "lab5_usart.h"

int plt_init(void)
{
    lab5_usart_init();
    return 0;
}

void plt_process(void)
{
    lab5_usart_process();
    plt_delay(10);
}
```
## Таблица спецификаторов sprintf 


| Спецификатор | Тип данных | Пример | Результат |
|:------------:|:-----------|:-------|:----------|
| `%d` или `%i` | `int` | `sprintf(buf, "%d", 42)` | `"42"` |
| `%u` | `unsigned int` | `sprintf(buf, "%u", 42)` | `"42"` |
| `%ld` | `long` | `sprintf(buf, "%ld", 42L)` | `"42"` |
| `%lu` | `unsigned long` | `sprintf(buf, "%lu", 42UL)` | `"42"` |
| `%x` | `unsigned int` (hex, строчные) | `sprintf(buf, "%x", 255)` | `"ff"` |
| `%X` | `unsigned int` (hex, заглавные) | `sprintf(buf, "%X", 255)` | `"FF"` |
| `%c` | `char` | `sprintf(buf, "%c", 'A')` | `"A"` |
| `%s` | `char*` (строка) | `sprintf(buf, "%s", "Hello")` | `"Hello"` |
| `%p` | `void*` (указатель) | `sprintf(buf, "%p", &var)` | `"0x20000001"` |
| `%%` | символ процента | `sprintf(buf, "%%")` | `"%"` |

### Модификаторы формата
| Модификатор | Описание | Пример | Результат |
|:-----------:|:---------|:-------|:----------|
| `%5d` | ширина поля 5 (вправо) | `sprintf(buf, "%5d", 42)` | `"   42"` |
| `%-5d` | ширина поля 5 (влево) | `sprintf(buf, "%-5d", 42)` | `"42   "` |
| `%05d` | дополнение нулями до 5 | `sprintf(buf, "%05d", 42)` | `"00042"` |
| `%.2f` | 2 знака после запятой | `sprintf(buf, "%.2f", 3.1415)` | `"3.14"` |
| `%08X` | HEX с 8 знаками, нули | `sprintf(buf, "%08X", 255)` | `"000000FF"` |
| `%#x` | префикс 0x для HEX | `sprintf(buf, "%#x", 255)` | `"0xff"` |

## Подключение через PuTTY

| Параметр | Значение |
|:---------|:---------|
| Connection type | Serial |
| Serial line | COM3 (узнайте ваш номер порта) |
| Speed | 115200 |

### Как узнать номер COM-порта

1. Откройте **Диспетчер устройств**
2. Разверните раздел **"Порты (COM и LPT)"**
3. Найдите **"STMicroelectronics STLink Virtual COM Port"**
4. Запомните номер (например, `COM3`)

## Ожидаемый вывод в терминале
После запуска программы и нажатия синей кнопки USER на плате:
Press: 1 | HEX: 0x1 | Time: 1024 ms
Press: 2 | HEX: 0x2 | Time: 2345 ms
Press: 3 | HEX: 0x3 | Time: 3567 ms
Press: 4 | HEX: 0x4 | Time: 4890 ms
Press: 5 | HEX: 0x5 | Time: 5123 ms